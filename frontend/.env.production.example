# Vercel production — Project Settings → Environment Variables
# Set AFTER Render backend is live. No trailing slash on backend URL.

REACT_APP_BACKEND_URL=https://razorstitch-api.onrender.com

# Optional — must match backend RAZORPAY_KEY_ID if checkout.js needs it client-side
# REACT_APP_RAZORPAY_KEY_ID=rzp_test_YOUR_KEY_ID
