{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Claim",
  "description": "A structured research claim for the Living Research Knowledge Base",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique identifier for the claim"
    },
    "statement": {
      "type": "string",
      "description": "The core factual statement or research finding"
    },
    "source_tier": {
      "type": "string",
      "enum": ["primary", "secondary", "tertiary"],
      "description": "Reliability tier of the source"
    },
    "citations": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of citations or references supporting the claim"
    },
    "policy_implication": {
      "type": "string",
      "description": "How this claim impacts simulator policies or priors"
    },
    "last_verified": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp of when this claim was last verified"
    },
    "conflict_ids": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "IDs of other claims that conflict with or nuance this claim"
    }
  },
  "required": ["id", "statement", "source_tier", "citations", "last_verified"]
}
